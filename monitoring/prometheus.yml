global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: "secure_llm_gateway"
    static_configs:
      - targets: ["gateway:8000", "host.docker.internal:8000"]
    metrics_path: "/metrics"
